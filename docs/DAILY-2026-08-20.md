# 工作日报（2026-08-20）

> 分支 **`feature/event-worker2`** · 本机 `/home/hycx/zyrao/visual-dps`  
> 主题：**ORT 线程收敛**、**0820 增量离线包**、**16/20/22 路 10 min 压测**、**现场部署脚本（sudo / retag）**。

---

## 一、背景与目标

- **0819 结论**：16 路 lag 健康但 worker-2 CPU ~194%；22/24 路 lag 顶满 2000；怀疑 ONNX/BLAS 默认多线程导致 CPU 虚高。
- **本日目标**：代码层 ORT/BLAS 收敛 + worker-2 共享 Session；打 **0820 增量包**（tag `07082d2`）供 153 升级；本机 **0820 镜像栈** 验证 worker-2/ONNX/线程 env 后做 **20 路 10 min** 稳态采样。

---

## 二、代码与部署脚本（入库）

### 1. 推理 / worker-2 性能（上午，已提交）

| 提交 | 内容 |
|------|------|
| `271ee93` | ORT/BLAS 线程默认、rtmlib Session 重绑 |
| `07082d2` | action_gate 共享 ONNX Session + 启动探针 |
| `ac73c52` | compose/.env.example 透传线程 env |
| `c15b2cc` | `sample_stability_run.py` / `render_stability_report.py` |

### 2. 0820 离线包与现场脚本（下午，本 commit）

| 项 | 说明 |
|----|------|
| `scripts/pack-deploy-incremental.sh` | 0820 增量包：UI + worker-1 + worker-2 tar，无 infer/redis |
| `scripts/deploy-only/OFFLINE-QUICKSTART-incremental-ui-worker2.md` | 对齐 0817 的完整 QUICKSTART 模板 |
| `scripts/deploy-only/retag-infer-images.sh` | 省略旧 tag 时自动 **0817→0813→0727**；`docker_cmd` |
| `scripts/lib/docker-cmd.sh` | 无 docker 组权限时自动 `sudo docker` |
| `pack-deploy-only.sh --keep-existing` | 增量刷新 app/weights 时不误删已有 tar |
| `infer-bind-mounts.sh` | 补 `core/ort_runtime.py` |
| install / load / verify | 统一走 `docker-cmd.sh` |

**0820 包路径**（本机）：`/home/hycx/zyrao/visual-dps-0820-deploy/`（~2.5G，3 业务 tar + app + weights）  
**镜像 tag**：`20260817-feature-eventworker2-07082d2`  
**infer**：现场 retag，不 rebuild。

---

## 三、本机 0820 栈部署与验收

| 项 | 结果 |
|----|------|
| compose 目录 | `visual-dps-0820-deploy/app` |
| UI | 重建镜像 `c82c3a2867a6`，已替换包内 UI tar |
| worker-2 | `07082d2`，仅 worker-2 运行 |
| infer ×20 | `gpu-onnx:07082d2`，bind 来自包内 `app/` |
| worker-2 ONNX | 日志 `backend=onnx session=ready probe_ok=True` |
| 线程 env | `ACTION_GATE_ORT_*=1`，infer `OMP/INFERENCE_ORT=1` |
| 注意 | `HOST_PROJECT_ROOT` 须改为现场 `app` 绝对路径并 **重启 UI**；勿与旧目录 compose 栈并行（容器名冲突） |

---

## 四、压测结果（ORT 优化栈 · tag 07082d2）

> 原始 JSON/Markdown 在 **`docs/test/`**（`.gitignore`，本机保留）。

### 1. 16 路 · 10 min（11:08–11:18）

| 指标 | 优化前 (0818) | **优化后** |
|------|---------------|------------|
| worker-2 CPU avg | ~194% | **~33%** |
| lag avg / max | 2.5 / 10 | **2.33 / 10** |
| 消费 | ~64 msg/s | **~69 msg/s** |
| 判定 | 稳定 ✓ | **稳定 ✓** |

报告：`docs/test/stability-10min-20260820-1108.md`

### 2. 22 路 · 10 min（11:42–11:52）

| 指标 | 0819 5min | **优化后 10min** |
|------|-----------|------------------|
| worker-2 CPU avg | ~239% | **~47.5%** |
| lag（稳态 ≥120s） | 顶满 2000 | **avg 8.4 / max 16** |
| 消费 | ~74 msg/s | **~74.2 msg/s** |

报告：`docs/test/stability-10min-22cam-20260820-1142.md`

### 3. 20 路 · 10 min（17:09–17:19 · **0820 部署包镜像栈**）

| 指标 | 值 |
|------|-----|
| 栈 | 0820 包 load + retag infer，**无 docker cp** |
| worker-2 CPU avg | **42.3%** |
| 消费速率 | **72.5 msg/s** |
| lag avg / max / last | **7.02 / 47 / 6** |
| infer CPU 合计 avg | **~1230%** |
| GPU 利用率 avg | **~85%** |
| 严格判定 | **需关注**（峰值 47 > 20；均值≤10，末段 lag≈6） |

报告：`docs/test/stability-10min-20260820-1709-20cam.md`

**解读**：相对 0819 22 路 lag 顶满 2000，**20 路在 0820 栈可运行**；偶发尖峰 47 未持续恶化。20 路可作为 0820 交付的 **推荐上限参考**（16 路仍最稳）。

---

## 五、结论

1. **ORT 线程 + 共享 Session** 有效：worker-2 CPU 16/20/22 路均 **~33–48%**（原 ~194–239%），消费速率不降。
2. **0820 增量包** 可交付：UI/worker/worker-2 + retag 脚本 + 完整 QUICKSTART；153 **原地覆盖** 部署目录或先 down 旧栈。
3. **20 路 10 min** 在 0820 镜像栈下 **可用但非严格稳定**（lag 峰值偶超 20）；生产建议 **≤16 路 sweet spot**，20 路需观察 lag。
4. infer **不必 rebuild**；worker-2 **必须** 用 07082d2 镜像。

---

## 六、待跟进

1. 153 rsync `visual-dps-0820-deploy`，核对 `HOST_PROJECT_ROOT` / `MEDIAMTX_PUBLIC_HOST` 后 install。
2. 可选补跑 **24 路 10 min**（脚本已就绪）。
3. `render_stability_report.py` 对照表路数标签按场景自动匹配（20 路报告仍写「16 路对照」需手改或改脚本）。
4. worker-2 镜像内打包 `verify_worker2_action_gate.py`（当前可用启动日志或 `python -c` 探针代替）。

---

## 七、Git 提交记录（本日）

| 提交 | 说明 |
|------|------|
| `271ee93` | perf(inference): ORT/BLAS 线程默认与 rtmlib Session 重绑 |
| `07082d2` | perf(pick_state): action_gate 共享 ONNX Session 与启动探针 |
| `ac73c52` | chore(deploy): compose 透传 ORT/BLAS 线程 env |
| `c15b2cc` | test(scripts): 稳定性连续采样与报告渲染 |
| `0dd338d` / `27daffa` | docs: 2026-08-20 工作日报 |
| **`efa7931`** | **chore(deploy):** 0820 增量包脚本、retag/sudo、QUICKSTART 模板 |

---

*2026-08-20 · 本机 hycx-B360-M-AORUS-PRO · RTX 3090 · i9-9900KF 16 逻辑核 · UI 8046*
