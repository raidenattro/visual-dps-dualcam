# 工作日报（2026-09-02）

> 分支 **`feature/dualcamera`** · 本机 `/home/hqit/workspace/visual-dps-dualcam-exp`  
> 主题：**event-worker 吞吐瓶颈排查 → 优化落地 → 6 巷道 overnight 观测**。pose **7.5Hz**（`pose_frame_interval: 2`），不能再降。  
> UI **8046** · worker `visual-dps-event-worker`（shard 0–7）+ `-b`（shard 8–15）· 推理 720 / RTMPose-M。

---

## 一、背景与目标

- **0901**：直播 3D 按 7.5Hz 适配因果平滑（`LIVE_KEEP_S=2.4s`，只滑质心再贴本帧形状），观感优先，未测多巷道 CPU。
- **本日现象**：
  1. 两个 worker 都在跑；某一瞬间 A 仅 **0.02%**、B **~52%** —— 不是挂了，是 CRC32(`aisle_id`) 把当时唯一在推理的 **aisle1 分到 shard 14 → B**。
  2. 开 **2 条巷道**（4 路相机）后 worker 跟不上；pose 不能再降。
- **本日目标**：用真实巷道 JSON + Redis pose snapshot 把每帧打点，定位吞吐花在哪。 **不改代码。**

硬约束未破：不改 `visual-dps` / `visual-dps-pick-state`；碰撞仍是 3D `contact_slots`；未成组不开推理。

---

## 二、现场拓扑（排查时）

| 巷道 | 相机 | logical shard | worker |
|------|------|---------------|--------|
| aisle1 | 1 / 2 | **14** | **B（8–15）** |
| aisle2 | 3 / 4 | 4 | A（0–7） |
| aisle3 | 5 / 6 | 2 | A |
| aisle4 | 7 / 8 | 1 | A |

哈希把 **4 条里的 3 条打给 A**。排查时实际在推理的是 aisle1 + aisle3，分落 B / A，Redis `lag≈0`，两边 CPU 各 **~60–70%**。这已经是「每 worker 一条巷道」的天花板，不是空闲。

`event-workers` 组里有一堆 idle 几十小时的旧 consumer（worker 重启残留）。当前 `lag≈0`，不是主因，但会干扰 `XPENDING` 判读。

---

## 三、预算（为什么 2 路必积压）

pose：**7.5Hz × 左右路** = 每巷道 **15 条** Redis Stream 消息/秒。

用 aisle1 / aisle3 的 `pose:snapshot` 在本机跑 `DualcamProcessor`（窗预热后稳态）：

| 场景 | 3D 调用 | 实测 | 每秒 CPU 时间 |
|------|---------|------|----------------|
| 1 巷道，配对成功 | 7.5 × `process_pair` | **45–52ms**（p95 ~80ms） | ~380ms/s → 约 0.4 核 |
| 1 巷道，经常配不上 | ~15 × `process_single` | **48–55ms**（几乎一样贵） | **~750ms/s** |
| 2 巷道进**同一** worker | 上面 ×2 | — | **0.8–1.5 核** → 积压 |

现场 docker stats（aisle1@B + aisle3@A，各 1 巷道）：

| 容器 | CPU |
|------|-----|
| `visual-dps-event-worker` | ~60–62% |
| `visual-dps-event-worker-b` | ~67–70% |
| `visual-dps-infer-{1,2,5,6}` | 各 ~40–50% |

推理侧写得上；积压在 worker 消费。同一进程再进第二条巷道，数学上超过 1 核。

---

## 四、一帧拆开（aisle1 snapshot，1 人）

`process_pair` ≈ **50ms**，**不是**三角化/贴墙：

| 步骤 | 每帧 | 约占比 | 代码 |
|------|------|--------|------|
| **2D 因果平滑 L+R** | **~34ms** | **~65%** | `LivePose2DSmoother.update` → `smooth_pose2d` |
| **3D 因果平滑** | **~24ms** | **~45%** | `LivePose3DSmoother.update`：整窗 `deepcopy` 两遍 + `smooth_frames` |
| 抬 17 关节 + `contact_slots` | **~2ms** | ~4% | `lift_point` / 32 货格 |
| `json.dumps(result)` | 0.3ms | — | event ~3KiB |
| `publish_event_frame` × L/R | **~4.4ms** | — | **每次 `from_url` 新建连接**再 `close` |

（2D+3D 加总会略大于 50ms，插桩有额外开销，量级正确。）

平滑窗：`LIVE_KEEP_S = 2.4s` → 7.5Hz 下约 **14 帧**。每帧对整窗重跑；`_gauss_smooth` 是纯 Python 双层循环。3D 窗 `deepcopy` 约 **1.8ms/次**。这是 0901 从 dump（25fps 整段）搬到直播的代价。

`process_single`（单路预览）同样走 2D 平滑 + 抬点 + 3D 平滑，**并不便宜**，只是不报贴墙。

---

## 五、架构上再砍一刀的点

### 1. 消费串行，且 `XREADGROUP count=1`（最大头）

`services/event_engine/worker.py` `_stream_loop`：**每次只取 1 条** pose，`asyncio.to_thread(process_*)` 完，再 `to_thread(publish)` 两次，才读下一条。同一 worker 上两条巷道的消息在一个 event loop 里排队，没有并行。

compose 里虽有 `POSE_STREAM_READ_COUNT=16`，**代码未读该变量**。

### 2. 每条 pose 重新扫巷道 JSON

`grouped_cameras()` 每个 payload 调一次：`list_aisles` 读全部 aisle 文件，再对每个 `load_aisle` **再读一遍**。4 条巷道约 **2.7ms/次**。15 pose/巷道/秒 → 白烧 ~40ms/s，巷道变多线性变差。

顺带：`_refresh_pair_window()` → `pair_window_sec()` 每条 pose 读 `runtime_config.json`（约 0.22ms）。

### 3. L/R 经常配不上，失败路径仍做全量 3D

配对窗日志 **0.400s**（`pair_window_periods=3` × pose 周期 0.133s）。现场左右路 `ts` 差经常 **0.6–1.2s**，于是 `process_single`。

`_note` **5 秒节流**，日志看起来偶尔一条，**实际几乎每帧都在算**。配不上 ≈ 7.5Hz × 2 路 × 50ms → **每巷道 ~0.75 核**，比配对成功的立体更伤吞吐。

### 4. 分片不均

`crc32(aisle_id) % 16`，不是负载均分。A 默认扛 aisle2/3/4，B 几乎只吃 aisle1。开「两条巷道」很容易两条都砸在 A 上（例如 aisle3+aisle4）。

---

## 六、明确不是瓶颈（避免改错）

- `lift_point` / 三角化 / `contact_slots`：~2ms
- 货格数量（每墙 32 cell）
- event JSON 大小
- 推理 7.5Hz 与 infer 容器 CPU（各 ~40%）
- Redis `XLEN=2000` 只是 `MAXLEN`；当时 `lag≈0` 只说明「每 worker 一条巷道还跟得上」，**没有余量**

---

## 七、若以后要加吞吐（只记方向，本日未改）

收益从大到小：

1. **平滑**：直播不要每帧重跑 2.4s 窗；One Euro / 只留 2–3 帧；preview 可不跑 3D 平滑。单项就可能把 50ms 砍到十几毫秒以内。
2. **配不上不要按 15Hz 做全量 3D**：等对侧或降频 preview；失败路径不要和 `process_pair` 一样贵。
3. **消费**：`XREADGROUP count>1`，按 `aisle_id` 批量配对；同一 worker 内两条巷道不要互相堵。
4. **缓存** `grouped_cameras`（按 mtime）；复用 Redis 连接发 overlay。
5. **分片**：CRC32 倾斜时按巷道 sticky 到空闲 worker，或显式指定 shard。

**定性：** 单巷道 7.5Hz 已经贴着 worker 算力墙；再开一路（尤其落到同一容器）不是「再挤一挤」，是超过 1 核。要扩路数必须先砍平滑/串行，而不是再降 pose。

---

## 八、吞吐优化落地（commit `9d1c02e`）

同日 evening 按 §七 方向改 worker，**不新开 worker**，目标撑住单 worker 多巷道：

| 改动 | 文件 | 说明 |
|------|------|------|
| 关直播 2D/3D 窗平滑 | `dualcam_processor.py` | `process_pair`/`process_single` 默认 `smooth_2d=False`、`smooth_3d=False`；贴墙仍用当帧抬点 + hold |
| 批量消费 | `worker.py` | `XREADGROUP count=POSE_STREAM_READ_COUNT`（compose 默认 16） |
| 不同巷道并行 | `dualcam_worker.py` | `asyncio.gather` + `to_thread`；同一巷道串行（processor 有状态） |
| `grouped_cameras` 缓存 | `aisle_store.py` | 按 mtime/size 失效 |
| overlay 复用 Redis | `event_bus.py` | `_sync_redis` 连接池 |
| 配对窗限频读盘 | `dualcam_worker.py` | 最多 2s 读一次 `runtime_config` |
| 启动清僵尸 | `worker.py` / `pose_bus.py` | `purge_stale_consumers`、`trim_idle_pose_streams`、`ack_own_pending` |

部署：`docker cp` 到 `visual-dps-event-worker` + `-b` 后 restart。  
副作用：2D overlay / 3D 更跟原始检测，可能更抖（需 Ctrl+Shift+R）。

---

## 九、lag 监控脚本 + overnight 实验

### 脚本

`scripts/monitor-pose-lag.sh` — tmux 挂着跑，每 **300s** 一行，日志 `logs/pose-lag-YYYYMMDD.log`：

```bash
cd /home/hqit/workspace/visual-dps-dualcam-exp
tmux new -s lag
./scripts/monitor-pose-lag.sh          # 一直跑
./scripts/monitor-pose-lag.sh 300 24h  # 限时
```

输出按 **worker A/B 汇总 + 各 shard 明细**：`consume`（消费 msg/s）、`ingress`（入流）、`d_lag`、`pending`、`xlen`、worker CPU、`infer` 容器数。  
`xlen≈2000` 是 Stream `MAXLEN`，不是积压；看 **lag / pending**。

### 观测窗口

| 项 | 值 |
|----|-----|
| 有效区间 | 2026-09-02 **18:42** → 09-03 **09:53**（约 15h，177 拍） |
| infer | **12** 路相机全程在线（6 巷道 × L/R） |
| WARN / CRITICAL | **0**（阈值 lag≥50 / ≥500） |

### Worker 汇总（177 拍）

| | A（shard 0–7） | B（shard 8–15） |
|---|---|---|
| consume | **47.3 msg/s**（p50 47.5） | **23.8 msg/s**（p50 23.9） |
| CPU | p50 **34.5%**（20–49%） | p50 **17.1%**（10–26%） |
| lag | p50 **0**，max **3** | p50 **0**，max **2** |
| pending | 0–4（多为 0/1） | 0–2 |

`consume ≈ ingress` 全程成立（|差|≤0.01），**入多少吃多少，无 Redis 积压**。  
A 吞吐约为 B 两倍，与 CRC32 分片 **4 巷道在 A、2 在 B** 一致。

### 活跃 shard（6 条巷道）

整晚只有 **6 个 shard** 有流量，各 **~11.7–12.0 msg/s**（低于账面 15 msg/s，是推理/推流未打满 7.5Hz×2，非 worker 吃不下）：

| shard | worker | 备注 |
|-------|--------|------|
| 1, 2, 4, 7 | A | 含原 aisle2/3/4；7 为后增巷道 |
| 13, 14 | B | 14 = 原 aisle1 |

### 结论

1. **优化后单 worker 可扛 4 巷道 ~47 msg/s、CPU ~35%、lag≈0**，优于排查时「每 worker 一条巷道 ~60–70% CPU」。
2. **6 巷道 / 12 路 infer 连续 15h 无积压**，验证 `9d1c02e` 目标达成。
3. 分片仍偏 A；再加巷道时新 `aisle_id` 易继续砸在 0–7。
4. `pending` 偶发 1–4 为 PEL 残留，lag=0 时可忽略；worker 重启后注意清僵尸 consumer。

原始日志：`logs/pose-lag-20260902.log`（本地，不入库）。

---

## 十、提交记录

| commit | 说明 |
|--------|------|
| `9d1c02e` | fix(dualcam): 关直播平滑 + 批量并行消费 |
| （本 commit） | chore: lag 监控脚本按 worker/shard 划分 + 日报补 overnight 结论 |

复现分片（只读）：

```bash
python3 -c 'import zlib;
[print(a, (zlib.crc32(a.encode())&0xffffffff)%16) for a in ("aisle1","aisle2","aisle3","aisle4")]'
# 0.02% 那次：只有 aisle1 在推理 → shard 14 → worker-b
```

---

*2026-09-02 · visual-dps-dualcam-exp · UI 8046 · 不改 visual-dps / visual-dps-pick-state*
