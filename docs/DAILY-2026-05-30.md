# 工作日报（2026-05-30）

## 一、GPU 推理镜像（inference-lite-gpu-onnx）

- **根因**：旧镜像混栈（`torch 2.12+cu130` + ORT `cudnn-cu12`）→ `CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH` → 推理回退 CPU；非 153 驱动版本问题。
- **锁栈重建**：`docker/install-gpu-onnx-pip.sh` — wget 交大源 torch 2.5.1+cu121、ORT 1.20.2、**ultralytics 8.4.0**（YOLO26 `Pose26`）。
- **构建辅助**：`docker/probe-torch-index.sh`、`scripts/probe-torch-mirror.sh`、`scripts/verify-gpu-onnx-image.sh`、`deploy/verify-gpu-onnx-content.sh`。
- **文档**：`docs/BUILD-inference-gpu-onnx.md`（镜像站探测、禁止项、锁栈表）。
- **运行时**：`services/nvidia_pip_cuda.py`、`rtmpose_onnx_backend` 预加载 cuDNN；`inference_worker` ORT preload。

## 二、153 联调（visual-dps-0529）

- 本机 `relay-to-remote-mediamtx.sh` 推 cam2～5 → `rtsp://192.168.1.153:8564`。
- 同步 gpu-onnx 镜像；cam3/4/5 RTMPose **device=cuda** 正常。
- cam2：`ultralytics 8.3` 缺 `Pose26` → 临时 `-ultra84` 补丁镜像 + 修正误设 `ENTRYPOINT=sleep`；正式包已重建 **8.4.0** 并 save/load 到 153。
- 用户确认：**153 推理已走 GPU**。

## 三、全量离线部署（唯一主流程）

- **入口**：`scripts/export-offline-one-shot.sh`（权重 → 构建 7 镜像 → 校验 gpu-onnx → 预检 → 打 complete 包）。
- **预检**：`scripts/preflight-offline-export.sh`；`export-offline-package.sh` 默认内嵌预检。
- **目标机**：`install.sh` 在 `docker load` 后校验 gpu-onnx；`deploy/regenerate-mediamtx-config.sh`。
- **清单**：`docs/OFFLINE-DEPLOY-CHECKLIST.md`；Skill / `AGENTS.md` / `OFFLINE-QUICKSTART` 收敛为仅全量离线。
- **第二实例**：`deploy/docker-compose.0529.override.example.yml`、`deploy/153.env.example`。

## 四、推理 / 流媒体（延续）

- infer 容器：`INFERENCE_RTSP_CAPTURE_BACKEND`、hwaccel 探测、GPU device_requests、挂载 `rtsp_capture` / `hwaccel_probe`。
- `153-restart-inference-gpu.sh`（API 需登录，现场多用 UI 内 Python 启停）。

## 五、待办

- 153 切正式 tag `20260529-165748-5c6b720`（替换 `-ultra84`）并 recreate UI 读新 `.env`。
- 源机跑通 `export-offline-one-shot.sh` 后 rsync 全量包做新机验收。
- ORT 加载 `libcudnn_adv.so.9` PATH 警告（可选 `LD_LIBRARY_PATH` 优化）。

## Git

| 项 | 内容 |
|----|------|
| 分支 | `main` |
| 本次提交 | `d4e35b4` — gpu-onnx 锁栈、全量离线一把过、153 联调文档 |
| 未提交 | `.env`（本地密钥） |
