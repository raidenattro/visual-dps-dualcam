# 工作日报（2026-06-28）

> 0529 GPU-ONNX 交付基线 commit：**`4841de6`**。本日工作在分支 **`test-from-4841de6a`** 上推进；离线评测 / benchmark 大改在 **`test-from-5c6b720`**，本分支尚未合入。  
> 统一交付镜像 tag：**`20260628-test-from-4841de6a-56f47b1`**（4841de6 基线 + 监控页矩阵 `56f47b1`）。

---

## 一、代码提交（已入库）

### 1. event-worker 碰撞控制台日志（`822cfbc`）

- `services/event_engine/worker.py`：`COLLISION_LOG=1` 时每帧输出 HIT / ALARM；标准墙钟 `YYYY-MM-DD HH:MM:SS`；优先 `video_time_sec`，否则 `frame_idx` + fps 推算片内时间。
- `event_worker.py`：启动时提示 `COLLISION_LOG=1`。
- `.gitignore`：忽略 `localdata/logs/`（现场 tee/重定向 worker 日志不入库）。

### 2. 监控页碰撞 / 事件矩阵对齐（`56f47b1`，参考 `test-from-5c6b720` / `98ddb1f`）

- **根因**：后端碰撞 token 为 `Box_{id}`，前端 ROI key 为 `{shelf}:{id}`，精确匹配导致侧栏矩阵与视频 ROI 不着色。
- `web/src/lib/annotation.js`：增强 `collisionMatchesBox`；新增 `boxMatchesAnyCollision`。
- `web/src/components/MonitorPreviewStage.jsx` / `.css`：矩阵态用 `boxMatchesAnyCollision`；侧栏「碰撞 N · 告警 N」；告警脉冲动画。
- `web/src/pages/MonitorPage.jsx`：Legacy WebSocket 网格同步改用 `boxMatchesAnyCollision`。
- 前端已 `npm run build`（build 号随 commit 写入 `version.json` / `build-info.js`）。

---

## 二、本地未提交配置（联调用，非交付态）

| 文件 | 变更 | 说明 |
|------|------|------|
| `app_config.json` | `pose_frame_interval` 3→**1** | 每帧跑 pose + 发 Redis |
| `localdata/runtime_config.json` | `backend` → **rtmpose_m**；`pose_frame_interval` 5→**1**；`height` 240 | cam1 手动 docker 推理联调 |

`.env` 现场项（不入库）：`MEDIAMTX_PUBLIC_HOST=192.168.31.12`；`UI_PORT=8046`；宿主机 UI + 手动推理。

---

## 三、本日联调动作与运行方式（4841de6a 分支）

### 栈组成

| 组件 | 方式 |
|------|------|
| Redis + MediaMTX | `docker compose up -d redis mediamtx` |
| UI | 宿主机 `PYTHONPATH=. uvicorn main:app --port 8046`（**勿** `app_factory:create_app --factory`） |
| event-worker | 宿主机 `COLLISION_LOG=1 PYTHONPATH=. python -u event_worker.py`，日志 tee 至 `localdata/logs/event-worker-*.log` |
| 推理 | 手动 `docker run` **`visual-dps-infer-cam1`**，镜像 **`visual-dps-inference-lite-gpu-onnx:20260529-165748-5c6b720-ultra84`**，`INFERENCE_BACKEND=rtmpose_m` |
| 推流 | cam1 单次 / 循环 ffmpeg → `rtsp://127.0.0.1:8554/cam1`；与标注对齐建议 **640×360**（`annotation_size`） |

### 宿主机环境要点

- UI / worker：`unset REDIS_URL` + `/etc/hosts` 增加 `127.0.0.1 redis`，或 UI 单独 `REDIS_URL=127.0.0.1` 且**仅用手动 docker 启推理**（避免 `127.0.0.1` 传入 infer 容器）。
- 推理容器 Redis：**`REDIS_HOST=redis`**（Docker 网络内），**不要** `REDIS_URL=@127.0.0.1`。
- 宿主机 UI：`unset MEDIAMTX_INTERNAL_HOST`；WebRTC 需 `MEDIAMTX_PUBLIC_HOST` 与 `mediamtx.yml` 的 `webrtcAdditionalHosts` 一致（本机 **192.168.31.12**），并放行 **8189 UDP/TCP**。

---

## 四、联调现象与结论（待产品/代码跟进）

| 现象 | 结论 |
|------|------|
| UI `REDIS_URL=127.0.0.1` 经 API 启推理 | infer 容器内连不上 Redis（127.0.0.1 指向容器自身） |
| `unset REDIS_URL` 且无 hosts | 宿主机 UI `redis` 解析失败 → LiveHub 报错 |
| WebRTC 黑屏 | 多为 `MEDIAMTX_PUBLIC_HOST` 与浏览器访问 IP 不一致或 ICE 8189 未通 |
| 标注与视频 ROI 错位 | cam1 标注 **640×360** + 源为 1.34 摄像头；推流 640×480 或换 MP4 需重标或改 scale |
| 单次推流结束 infer 不退出 | **设计如此**：RTSP 模式断流后空转 + 重连（404 DESCRIBE），非文件 EOF；需 `docker stop visual-dps-infer-cam1` |
| 推理启动 → 首包 pose 延迟 | 冷启动模型加载 + FFmpeg NVDEC 失败回退 OpenCV + 首帧 warmup；日志停在「推理参数」后才有 XADD |
| infer 日志「流地址与标注不一致」 | 警告，不阻断；测试 MP4 与 `source_info` 原 RTSP 不同 |

---

## 五、153 负载测试（WSL 推流 + 多路 infer）

| 场景 | Redis lag | 碰撞 overlay | 结论 |
|------|-----------|--------------|------|
| **12 路** infer | ~2000 | 滞后 ~15s | worker 消费跟不上，前端碰撞明显延迟 |
| **4～5 路** infer | 0 | 基本实时 | 4090 + rtmpose_m 可支撑约 5 路实时碰撞 |

- 详细记录：`docs/LOAD-TEST-12CAM-2026-06-28.md`、`docs/LOAD-TEST-5CAM-2026-06-28.md`
- 推流：WSL ffmpeg 循环 MP4 → `rtsp://192.168.1.153:8554/cam*`
- MediaMTX path 查询须进容器（**9997 未映射宿主机**）：`docker exec visual-dps-mediamtx wget -qO- http://127.0.0.1:9997/v3/paths/list`

### 联调发现：ALARM 不触发（仅 HIT）

| 项 | 说明 |
|----|------|
| **现象** | infer 重启后 worker 有 HIT 无 ALARM |
| **根因** | infer 重启 `frame_idx` 回绕；worker 内存 `_box_last_alarm_frame`（如 10656）未清，`frame_idx - last_alarm < cooldown` 长期失败 |
| **连带** | `alarm_cooldown_frames: 0` 被 `0 or 12` 误读为 12 |

### 代码修复（本地已改，153 需 `docker cp` event-worker）

- `services/event_engine/worker.py`：`alarm_cooldown_frames=0` 正确读取（`in infer_cfg` 分支 + `max(0, …)`）
- `services/event_engine/collision.py`：`alarm_cooldown_frames = max(0, int(...))`
- **待跟进**：infer 带 `run_id` / worker 在 `frame_idx` 回退时重置 processor（未实现）

---

## 六、镜像构建（tag `20260628-test-from-4841de6a-56f47b1`）

- 构建记录：`docs/BUILD-20260628-test-from-4841de6a-56f47b1.md`
- 7 个镜像：redis:7、mediamtx:1.11.3、UI、event-worker、inference-lite / lite-gpu / lite-gpu-onnx
- `.env`：`VISUAL_DPS_IMAGE_TAG=20260628-test-from-4841de6a-56f47b1`；153 联调 `UI_PORT=8046`、`MEDIAMTX_PUBLIC_HOST=192.168.1.153`

---

## 七、离线包（0628，分镜像 + 纯部署包）

参考 0529 布局，产出在 **`~/workspace/visual-dps-0628`** 与 **`~/workspace/visual-dps-0628-deploy`**（非仓库内 dist）。

### 7.1 分镜像包 `visual-dps-0628`（~12G）

| 项 | 内容 |
|----|------|
| 布局 | **7 个独立 `.tar`**（非合并 `bundle.tar`），便于排查缺失镜像 |
| 脚本 | `export-images.sh`、`load-images.sh`、`verify-package.sh`（OCI `manifest.json` 校验） |
| 合并包 | `docker-images/images.tar.gz`（12G，用户侧另存） |
| 文档 | `DEPLOY-SERVICES-0628.md`、`PACKAGE_INFO.txt` |

### 7.2 纯部署包 `visual-dps-0628-deploy`（~331M，不含镜像）

| 项 | 内容 |
|----|------|
| 内容 | `app/` + `weights/` + `install.sh` / `verify-images.sh` / `verify-package.sh` |
| 安装 | 目标机先 `docker load` 7 镜像 → `./verify-images.sh` → `./install.sh --host <IP> --stop-infer` |
| 脚本 | `pack-deploy.sh` 源机重打；`HOST_PROJECT_ROOT` 默认写 `/opt/visual-dps`（**目标机须改实际 app 路径**） |

### 7.3 U 盘拷贝

- 路径：`/media/hqit/usb/visual-dps-0628/`
- 文件：`images.tar.gz`（12G）、`visual-dps-0628-deploy.tar.gz`（270M）

---

## 八、153 离线包部署验收（晚间）

| 步骤 | 结果 |
|------|------|
| 清空旧容器/镜像 → `docker load` 7 镜像 | OK |
| `./install.sh --host 192.168.1.153` | UI / redis / mediamtx / event-worker 可起 |
| 前端访问 `192.168.1.153:8046` | OK |
| WSL 推流 cam1～8 | 需推到 **153**（非 193）；8554 已映射 |
| 启动推理 | **失败**（见下） |

### 部署踩坑与根因

| 问题 | 根因 | 临时处理 |
|------|------|----------|
| `mediamtx.yml` mount 报 not a directory | 文件不存在时 compose 先起 mediamtx，Docker 建同名**目录** | `sudo rm -rf app/localdata/mediamtx.yml` 后重新生成 |
| `regenerate-mediamtx-config.sh` 无输出、不生成文件 | `docker run` **缺 `-i`**，heredoc 未传入容器 | 手动 `python3 -c apply_mediamtx(...)` 或仓库加 `-i` |
| 前端「智能检测服务暂时不可用」 | infer 容器 **Created 未 Running**；bind mount 失败 | 见下 |
| `inference_worker.py` 等为**目录** | 部署包无推理源码；UI 启 infer 时挂载宿主机文件，不存在则 Docker 建目录 | 从 `visual-dps` 仓库拷 `inference_worker.py`、`core/config.py`、`services/*.py` 到 `app/` |
| `HOST_PROJECT_ROOT=/opt/visual-dps` | 打包脚本写死，153 实际路径为 `~/workspace/visual-dps-0628-deploy/app` | 改 `app/.env` 后 `restart visual-dps-ui` |

---

## 九、未做 / 后续

- [ ] **`deploy/regenerate-mediamtx-config.sh`**：`docker run --rm -i`（heredoc 修复）
- [ ] **`pack-deploy.sh`**：打包推理挂载所需源码；`HOST_PROJECT_ROOT` 改为 install 时按实际路径写入
- [ ] **离线包**：install 前校验 `mediamtx.yml` 为文件；infer 启动前校验挂载路径存在
- [ ] event-worker **`frame_idx` 回绕** 后重置 collision 状态（infer `run_id`）
- [ ] 合入或 cherry-pick `test-from-5c6b720` 离线 benchmark / 告警参数 / media 时间轴等
- [ ] `inference_container_service`：宿主机 UI 启推理时 Redis URL 改写（127.0.0.1 → redis）
- [ ] 153 离线包推理链路验收通过（拷源码 + 改 `HOST_PROJECT_ROOT` 后复测）

---

## Git

| 项 | 内容 |
|----|------|
| 分支 | `test-from-4841de6a` |
| 已提交 | `822cfbc` 碰撞日志；`56f47b1` 监控页矩阵对齐 |
| 本地未提交 | `services/event_engine/worker.py`、`collision.py`（alarm_cooldown）；`app_config.json`、`runtime_config.json` |
| 仓库外产物 | `~/workspace/visual-dps-0628`、`~/workspace/visual-dps-0628-deploy` |

---

## 验证命令速查（153 / 本机）

```bash
# 栈与版本
docker ps | grep visual-dps
curl -s http://127.0.0.1:8046/api/version | python3 -m json.tool

# MediaMTX path（须进容器）
docker exec visual-dps-mediamtx wget -qO- http://127.0.0.1:9997/v3/paths/list

# 推流 / 推理
ffprobe -rtsp_transport tcp -i rtsp://127.0.0.1:8554/cam1
docker logs -f visual-dps-infer-cam1
docker exec visual-dps-redis redis-cli -a "$REDIS_PASSWORD" XINFO GROUPS pose:stream

# 离线包
./verify-images.sh && ./verify-package.sh
curl -s -X POST http://127.0.0.1:8046/api/cameras/cam1/inference/start | python3 -m json.tool
```
