# 工作日报（2026-08-07 后续 · 2026-08-12）

> 分支 **`exp/event-confirm`**。主题：试验场结论收口；算法成果并入 **`visual-dps-xugang-dev`**（产品仓 `exp/event-worker-2`）。

---

## 一、今天做了什么

1. **产品化交接**：确认试验场只作研发；运行时不再依赖本仓路径  
2. 对齐 xugang-dev 集成约定：部署包根布局不动；Git 在 `app/`；双 worker 同 `pose:stream` / `event-workers`，靠启停对照  
3. 本仓无新算法改动（工作区干净）；轨迹诊断与 backlog 仍以 8.7 日报 / HANDOVER 为准

## 二、交接给产品仓的内容

| 项 | 说明 |
|----|------|
| 算法 | `pipeline` / `experts` / `features` + `v5_gated` |
| 模型 | `v5_base/model.json`、`action_gate_v1/model.joblib` |
| 不变量 | 15fps；手腕门槛两套（trigger 0.15 / 角度 0.3）；空帧推进时序 |

产品侧仓库：`visual-dps-xugang-dev/app` → 远程 **HQIT/visual-dps** 分支 `exp/event-worker-2`。

## 三、Git

| Commit | 说明 |
|--------|------|
| （本日） | `docs:` 本日报 + HANDOVER 产品化交接说明 |

历史相关：`e96d3cd` 轨迹结论；`70c8c3b` 门控；`17e060e` v4 标注。

## 四、待跟进（试验场）

1. 非骨架路径：框 ROI 时序 / 手检测 / TAL（见 HANDOVER）  
2. 生产门控：先 A 或 A+B 的最终拍板（可在产品仓联调后定）  
3. 本分支 push 至 `SUGE2016/visual-dps-pick-state` 的 `exp/event-confirm`
