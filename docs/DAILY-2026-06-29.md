# 工作日报（2026-06-29）

> 分支 **`test-from-4841de6a`**，当日 HEAD **`ce4fe0a`**（含 event-worker 初始化修复）。  
> **最终交付镜像 tag**：**`20260629-test-from-4841de6a-ce4fe0a`**（bc66c0c 之上重打 UI/event；构建记录见 [`BUILD-20260629-test-from-4841de6a-bc66c0c.md`](./BUILD-20260629-test-from-4841de6a-bc66c0c.md)，tag 名以 `.env` 为准）。

---

## 一、代码提交（已入库）

### 1. RTMPose 双下拉与 `models.det`（`d86450b`）

- `model_registry` / `rtmpose_onnx_backend` / `runtime_config_service`：RTMDet **nano / M** 可独立配置。
- 前端 `InferenceModelFields.jsx`：RTMPose 时显示检测器下拉；YOLO 不显示 det。
- 权重脚本增加 `rtmdet_m` 下载。

### 2. 总览批量启停推理（`9ef8244`）

- API：`POST /api/inference/start-all`、`stop-all`。
- 前端总览页「全部启动/停止检测」。
- 推理状态写入 `rtm_det`，监控页模型标签与容器实际一致。

### 3. 容器时区与墙钟时间（`0eb8e33`）

- `.env` / compose：`TZ=Asia/Shanghai`，挂载 `/etc/localtime`。
- 新增 `services/wall_clock.py`；碰撞日志、回调 `time=` 与宿主机 `date` 对齐（CST）。
- `callback_reporter` / `event_engine/worker` 统一使用本地时间字符串。

### 4. 设置页碰撞告警参数（`bc66c0c`）

- **全局可配**：`inference.alarm_min_consecutive_frames`、`inference.alarm_cooldown_frames`（写入 `localdata/runtime_config.json`）。
- `runtime_config_service`：扩展 `PUBLIC_KEYS`；`alarm_cooldown_frames=0` 合法；`get_merged_inference_section()` 供 event-worker 读取。
- `event_engine/worker.py`：监听 `runtime_config.json` mtime，告警门控**热更新**（改设置后无需重启 worker，异常时可 `docker restart visual-dps-event-worker`）。
- 前端设置页新增「碰撞告警」分区；所有推理/碰撞字段增加 **?** 提示（字段说明 + 需重启的服务）。
- 文件：`SettingsPage.jsx`、`cameraSettings.js`、`InferenceModelFields.jsx`、`CameraSetupDrawer.jsx`、`FieldHint.jsx`。

### 5. event-worker 启动修复（`ce4fe0a`）

- **根因**：`_apply_alarm_settings()` 在 `_contexts` 初始化之前调用 → `AttributeError`，容器 Restarting。
- **修复**：`self._contexts = {}` 提前到 `_apply_alarm_settings()` 之前。
- 顺带修正 `callback_reporter.py` 缩进错误；补齐 `wall_clock.py` 热更新部署。

---

## 二、镜像构建与 tag

| 项 | 值 |
|----|-----|
| 中间 tag | `20260629-test-from-4841de6a-bc66c0c`（设置页告警门控等） |
| **最终 tag** | **`20260629-test-from-4841de6a-ce4fe0a`**（含 `ce4fe0a` worker 修复 + UI/event 重打） |
| `.env` 四行 | `VISUAL_DPS_IMAGE_TAG` + 三个 `INFERENCE_LITE_*` 已同步为 **ce4fe0a** |
| 已构建镜像 | `visual-dps-visual-dps-ui`、`event-worker`、`inference-lite-gpu`、`inference-lite-gpu-onnx` |
| 未打入 U 盘 tar | `redis:7`、`mediamtx:1.11.3`、`inference-lite`（CPU）；GPU 现网可 `--skip-lite-cpu` |
| 注意 | 构建脚本会先 `source .env`；**勿只 export TAG 而不改 .env**，否则仍会打到旧 tag |

全量构建一条命令（tag 换 **ce4fe0a**）：

```bash
cd ~/workspace/visual-dps && export DOCKER_TAG_ALSO_LATEST=1 && \
./scripts/build-inference-lite-gpu-image.sh && \
./scripts/build-inference-lite-gpu-onnx-image.sh && \
./scripts/verify-gpu-onnx-image.sh visual-dps-inference-lite-gpu-onnx:20260629-test-from-4841de6a-ce4fe0a && \
./scripts/build-ui-image.sh --up
```

本机 `docker images | grep ce4fe0a`（晚间验收）：

| 镜像 | SIZE |
|------|------|
| visual-dps-visual-dps-ui | 1.21GB |
| visual-dps-event-worker | 811MB |
| visual-dps-inference-lite-gpu | 5.92GB |
| visual-dps-inference-lite-gpu-onnx | 17.2GB |

---

## 三、本日联调与环境

### 栈（本机）

```bash
docker compose up -d redis mediamtx visual-dps-ui visual-dps-event-worker
# UI_PORT=8046（.env）
```

| 组件 | 镜像 tag | 状态 |
|------|----------|------|
| visual-dps-ui | ce4fe0a | 正常 |
| visual-dps-event-worker | ce4fe0a | 修复后 Up；`COLLISION_LOG=1` 有 HIT/ALARM |
| 推理 cam1–cam8 | ce4fe0a onnx | 多路并行；全局 **rtmpose_m + RTMDet-M** |

### 推流（cam1–cam8）

- MediaMTX API（本机映射 **9997**）：八路 `ready=True`，`tracks=H264`，`source=rtspSession`。
- 播放：`rtsp://127.0.0.1:8554/cam1` … `cam8`。

### 全局运行时配置（`localdata/runtime_config.json`）

```json
{
  "models": { "backend": "rtmpose_m", "det": "m" },
  "inference": {
    "frame_rate": 15,
    "height": 240,
    "pose_frame_interval": 2,
    "alarm_min_consecutive_frames": 3,
    "alarm_cooldown_frames": 0
  },
  "debug-info": { "enabled": false }
}
```

---

## 四、问题排查与结论

| 现象 | 根因 | 处理 |
|------|------|------|
| event-worker Restarting | `_contexts` 未初始化即 `_apply_alarm_settings` | 已修并 `docker cp` + restart |
| worker 曾报 `IndentationError` / 缺 `wall_clock` | 热更新未打镜像 | 已 cp 修复；交付需重打 **ce4fe0a** 镜像 |
| cam3 CPU ~93%，其它 ~35% | `camera_ips.json` 中 cam3 单独 `pose_frame_interval:1` | 已删全部摄像头 `settings` 写死项 |
| cam4/cam6 仍跑 RTMPose-T | 旧 infer 容器未重启 | 需对 cam3/4/6 **停启智能检测** 后才会全局 M + interval 2 |
| 构建 tag 仍为 `9ef8244` | `.env` 未改，`load_build_env` 覆盖 export | 已统一为 **ce4fe0a** |
| U 盘 `/media/hqit/usb` I/O 错误 | 拔插后设备名变（sdc→sdb），挂载点仍指向旧设备 | `sudo umount -l` 后重挂 `/dev/sdb1` |
| `docker save` 合并 tar 仅 ~6.2G | 多镜像**层去重**；`docker images` SIZE 为虚拟大小（共享层重复计入） | manifest 内 **4 个 RepoTags**；`docker load` 后展开为 4 镜像 |
| Java 回调 `Connection refused` | `callback_ip=127.0.0.1` 容器内无 Java | 预期；联调需改 `reporting` 或 mock |
| GPU 监控 13% 含义 | `utilization.gpu` 为**整卡** SM 利用率，非显存占比 | 按 PID 过滤可见单进程显存 |

### `localdata/camera_ips.json` 清理

- 已删除 cam3 / cam4 / cam6 的 `settings` 块（姿态间隔、单独 backend 等联调残留）。
- 现 **14 路均无 `settings`**，推理统一走 `runtime_config.json`。
- **待办**：重启仍带旧 env 的 `visual-dps-infer-cam3`（及 cam4/cam6）以生效。

---

## 五、资源观测（8 路 infer + rtmpose_m）

- 单路 CPU（docker stats）：约 **30–36%**（interval=2）；cam3 在 interval=1 时约 **93%**。
- 单路内存：约 **900–970 MiB**。
- GPU：整卡利用率随多路叠加；显存约 **5GB 级/卡**（4090 24GB 场景下 8 路可跑，碰撞延迟需结合 Redis lag 观察）。

监控命令（2s 刷新，整卡 GPU + 推理容器 CPU/内存）：

```bash
watch -n 2 'echo "=== GPU ==="; nvidia-smi --query-gpu=index,utilization.gpu,memory.used,memory.total --format=csv,noheader; echo; echo "=== 推理容器 ==="; docker stats $(docker ps -q --filter label=visual-dps.role=inference) --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.MemPerc}}"'
```

---

## 六、纯部署包与 U 盘交付（0629-jj）

参考 0628 布局，产出在 **`~/workspace/visual-dps-0629-deploy`**（仓库外）及 U 盘 **`/media/hqit/usb/0629-jj/`**。

### 6.1 纯部署包 `visual-dps-0629-deploy`（~435M，不含镜像）

| 项 | 内容 |
|----|------|
| 脚本 | `scripts/pack-deploy-only.sh` → 拷贝 `scripts/deploy-only/{install,verify-package,verify-images}.sh` |
| 内容 | `app/`（含推理 bind mount 源码）+ `weights/` + install/verify 脚本 |
| tag | `VISUAL_DPS_IMAGE_TAG=20260629-test-from-4841de6a-ce4fe0a` |
| `HOST_PROJECT_ROOT` | 打包默认 `/home/hqit/workspace/visual-dps-0629-deploy/app`（**153 须改实际路径**） |
| 安装 | 先 load 镜像 → `./verify-images.sh --skip-lite-cpu` → `./install.sh --host <IP> --stop-infer` |

重打部署包：

```bash
~/workspace/visual-dps/scripts/pack-deploy-only.sh \
  --host-root ~/workspace/visual-dps-0629-deploy/app
```

### 6.2 U 盘 `0629-jj/` 布局

| 路径 | 大小 | 说明 |
|------|------|------|
| `visual-dps-0629-deploy/` | ~452M | 纯部署包 |
| `docker-images/ce4fe0a-images.tar` | ~6.2G | **1 个 tar，内含 4 镜像**（层去重） |
| `docker-images/load-images.sh` | — | 目标机 `docker load` |
| `README.txt` | — | 简要步骤 |

校验 tar 内镜像数：

```bash
tar -xOf .../ce4fe0a-images.tar manifest.json \
  | python3 -c "import json,sys; m=json.load(sys.stdin); [print(t) for x in m for t in x['RepoTags']]"
```

### 6.3 192.168.1.117 传输验收

- `rsync` 传输 `ce4fe0a-images.tar`（6.2G）→ `hqit@192.168.1.117:~/0629-jj/docker-images/`
- 远程 `docker load` 输出 4 行 `Loaded image`，`docker images | grep ce4fe0a` 计数 **4**
- 117 上展开后 SIZE 与源机 `docker images` 列不完全一致（显示口径不同），属正常

### 6.4 U 盘挂载注意

- Linux 路径：**`/media/hqit/usb`**（无 `/usb`）
- 拔插后设备名可能变；若 I/O 错误：

```bash
lsblk | grep -E 'sd[b-z]'
sudo umount -l /media/hqit/usb
sudo mount -t exfat -o uid=1000,gid=1000,umask=022 /dev/sdb1 /media/hqit/usb
sync   # 拔盘前
```

---

## 七、待跟进

1. **153 现网**：load **ce4fe0a** 四镜像 + 同步 `visual-dps-0629-deploy`，改 `HOST_PROJECT_ROOT` 后 `./install.sh --stop-infer`。
2. **cam3/4/6 推理容器重启**，确认日志为 `pose_frame_interval=2` 且 `RTMPose-M`。
3. **碰撞回调 `reporting`** 尚未上 Settings UI（仅告警门控两项已上）；Java 地址仍改 `app_config.json`。
4. **cam5 标注** `cam5.json` 仅 12 行，几乎无货框 → 该路无矩阵/碰撞事件（预期）。
5. 可选：U 盘改为 **4 个独立 `.tar` + 压缩包**（体积更大、便于肉眼核对，见 pack 脚本或 Agent 会话命令）。

---

## 八、相关文档

| 文档 | 说明 |
|------|------|
| [`BUILD-20260629-test-from-4841de6a-bc66c0c.md`](./BUILD-20260629-test-from-4841de6a-bc66c0c.md) | 构建命令（最终 tag 用 **ce4fe0a**） |
| `scripts/pack-deploy-only.sh` | 纯部署包打包（不含镜像） |
| `scripts/deploy-only/` | 部署包 install / verify 脚本模板 |
| [`DAILY-2026-06-28.md`](./DAILY-2026-06-28.md) | 前日矩阵对齐、12/5 路压测 |
| [`LOAD-TEST-12CAM-2026-06-28.md`](./LOAD-TEST-12CAM-2026-06-28.md) | 12 路负载参考 |
