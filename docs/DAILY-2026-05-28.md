# 工作日报（2026-05-28）

## 一、推理与 YOLO

- 打通 **YOLO26-pose** 后端：`model_registry` 预设、`yolo_pose_backend`、lite 镜像集成 ultralytics。
- 修复推理无法启动：`core/config.py` 去掉废弃 mmpose 必填项；推理容器传入 **`INFERENCE_USE_GPU`**，挂载宿主机 **`core/config.py`**。
- 修复 `start_inference_container` 写入状态时 **`backend` 未定义**。
- 本机验证 **cam2 + yolo26s_pose** 可在 CPU 下跑通；权重脚本与 `.env` 推理镜像变量已对齐。

## 二、流媒体 / 联调

- 支持 **cam1～cam4** MP4 循环推流到 MediaMTX（`start-mp4-rtsp-multi.sh`）。
- 本机 WSL → **192.168.0.204** 直推 cam1～cam3：`relay-to-remote-mediamtx.sh publish`；204 上 ffprobe 确认三路 **H.264**。
- **WebRTC 黑屏（ICE 8189）已解决**：
  - `mediamtx_proxy`：从请求 **Host** 推断浏览器可达 IP，SDP 中改写 RFC1918 及 **`127.0.0.1`**。
  - 新增 `scripts/fix-webrtc-public-host.sh`（`.env` + `webrtcAdditionalHosts` + 重启 mediamtx/UI）。
  - 204 部署目录 `visual-dps-0528` 已热更新代理并重启容器；监控页 WebRTC 预览正常。
- 无 GPU 本机用 **lite-gpu-onnx + CPU** 做功能验证；性能/GPU 联调在 **204**。

## 三、标注 UI

- 货位 **内部拖动平移**（`cell-body`），角点命中缩小，指针监听用 **ref** 避免拖动中断。
- 侧栏 **「删除货位」** 删除单格 ROI（无键盘快捷键）。
- 点击与拖动分离（约 4px 移动阈值）。

## 四、交付与工程

- 离线包增强：`export-offline-complete.sh`、`docker-compose.deploy.yml`、`verify-package.sh`、`offline.env.example`；更新 `install.sh`、`PACKAGE-MANIFEST.md`、`OFFLINE-QUICKSTART.md`。
- 摄像头/设置页字段说明（`FieldHint`）、流类型表单整理。

## 五、待办 / 风险

- 本机 **无 NVIDIA** 时 YOLO 仅适合功能验证；吞吐与 GPU 路径在 **204** 继续测。
- `compose up` 需 **`VISUAL_DPS_IMAGE_TAG`**（勿仅依赖 `latest`）。
- 跨机 WebRTC：防火墙放行 **8045、8889、8189 TCP+UDP**；`MEDIAMTX_PUBLIC_HOST` 须为浏览器访问的局域网 IP。
- WSL → 204 中继进程退出后需重跑 `relay-to-remote-mediamtx.sh publish`。

## Git

| 项 | 内容 |
|----|------|
| 分支 | `experiment/no-mmpose-yolo-pose` |
| 提交 1 | `6bbb1d1` — YOLO26-pose、离线交付、标注 ROI |
| 提交 2 | `9657812` — WebRTC SDP 改写、离线完整包、日报 |
| 远程 | `origin/experiment/no-mmpose-yolo-pose` |

未提交 `.env`（仍在 `.gitignore`）。
